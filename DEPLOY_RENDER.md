# Деплой на Render

Пошаговая инструкция для размещения вашего сайта на платформе Render.

## Шаг 1: Подготовка кода

Убедитесь, что все изменения сохранены в Git:

```bash
git add .
git commit -m "Готово к деплою"
git push
```

Если у вас еще нет Git репозитория на GitHub:

1. Создайте новый репозиторий на [github.com](https://github.com/new)
2. Выполните команды (замените `ваш-username` и `ваш-репозиторий`):

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git push -u origin main
```

## Шаг 2: Создание сервиса на Render

1. Зайдите на [render.com](https://render.com) и войдите (или зарегистрируйтесь)

2. Нажмите **"New +"** → **"Web Service"**

3. Подключите ваш GitHub репозиторий:
   - Если это первый раз, разрешите Render доступ к GitHub
   - Выберите нужный репозиторий из списка

4. Настройте сервис:
   - **Name**: выберите имя для сайта (например, `my-ecommerce-site`)
   - **Region**: выберите ближайший регион (например, Frankfurt для России)
   - **Branch**: `main`
   - **Root Directory**: оставьте пустым
   - **Runtime**: Node
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm start`

5. Выберите план:
   - **Free** (бесплатный) — подходит для тестирования
     - Сайт "засыпает" после 15 минут бездействия
     - Первая загрузка после сна займет ~30 секунд
   - **Starter** ($7/месяц) — для продакшена
     - Сайт работает постоянно
     - Быстрая загрузка

6. Нажмите **"Create Web Service"**

## Шаг 3: Деплой

Render автоматически:
1. Скачает ваш код
2. Установит зависимости
3. Соберет проект
4. Запустит сервер

Процесс займет 2-5 минут. Следите за прогрессом в логах на странице сервиса.

## Шаг 4: Готово!

После успешного деплоя:
- Ваш сайт будет доступен по адресу: `https://ваше-имя.onrender.com`
- Render покажет вам ссылку вверху страницы

## Автоматические обновления

При каждом `git push` в ваш репозиторий Render автоматически:
1. Обнаружит изменения
2. Пересоберет проект
3. Обновит сайт

## Переменные окружения (если нужны)

Если вам нужно добавить секретные ключи или настройки:

1. На странице вашего сервиса перейдите в **"Environment"**
2. Нажмите **"Add Environment Variable"**
3. Добавьте нужные переменные (например, API ключи)

**Примечание**: `NODE_ENV=production` уже настроен автоматически

## Настройка своего домена (опционально)

1. На странице сервиса перейдите в **"Settings"**
2. Найдите секцию **"Custom Domain"**
3. Нажмите **"Add Custom Domain"**
4. Следуйте инструкциям для настройки DNS

## Проблемы?

Если что-то пошло не так:

1. Проверьте логи деплоя на странице сервиса
2. Убедитесь, что проект собирается локально: `npm run build`
3. Проверьте, что команды в настройках правильные

## Полезные команды для тестирования локально

```bash
# Проверить сборку
npm run build

# Запустить production версию локально
npm start
```

---

**Готово!** Ваш сайт теперь доступен в интернете 🚀
